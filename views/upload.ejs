<!DOCTYPE html>
<html>
<head>
    <title>Upload VCF to MongoDB</title>
    <!-- Load Bootstrap CSS -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <!-- Custom CSS -->
    <style>
        body {
            background-color: #f9f9f9;
        }

        .upload-form {
            padding: 40px;
            background-color: #fff;
            border-radius: 10px;
            box-shadow: 0px 2px 10px rgba(0, 0, 0, 0.1);
        }

        .upload-title {
            font-family: 'Dancing Script', cursive;
            font-size: 36px;
            text-align: center;
            color: #ff6b6b;
            margin-bottom: 30px;
        }

        .upload-button {
            background-color: #ff6b6b;
            border-color: #ff6b6b;
        }

        .upload-button:hover {
            background-color: #ff5252;
            border-color: #ff5252;
        }
    </style>
</head>
<body>
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="upload-form container container-md">
                    <h1 class="upload-title">Upload VCF File</h1>
                    <form method="POST" action="/upload" enctype="multipart/form-data">
                        <div class="form-group">
                            <label for="vcfFile">Select VCF File</label>
                            <input type="file" class="form-control-file" id="vcfFile" name="vcfFile" accept=".vcf" required>
                        </div>
                        <div class="text-center">
                            <button type="submit" class="btn btn-primary btn-lg upload-button">Upload</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <!-- Load Google Fonts for custom font -->
    <link href="https://fonts.googleapis.com/css2?family=Dancing+Script&display=swap" rel="stylesheet">

    <!-- Load Bootstrap JS (popper.js and jQuery are required) -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.1/dist/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
